[package]
name = "template"
version = "0.1.0"
edition = "2024"

[dependencies]
libs.workspace = true
proconio = "0.5.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(actester)'] }
